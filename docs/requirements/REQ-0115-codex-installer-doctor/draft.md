# REQ-0115 — Codex installation and doctor paths

Install, update, uninstall, and doctor functions for the Codex provider. Mirrors the Claude installer API (same options shape, same return shape) but simpler — no hooks directory, no settings.json merge. Creates Codex instruction directory, generates instruction files from team specs/instances.
