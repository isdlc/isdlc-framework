# REQ-0122 — Provider support matrix

Frozen data module documenting per-feature Claude vs Codex comparison with support status, governance strength deltas per checkpoint, and known Codex limitations. Exports getProviderSupportMatrix(), getGovernanceDeltas(), and getKnownLimitations() for downstream tooling and documentation generation.
