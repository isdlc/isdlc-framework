# REQ-0116 — Codex instruction projection service

Full instruction projection for Codex tasks. Consumes core models (team spec, instance config, content classification, skill injection plan) and assembles them into a single markdown instruction bundle per task. System-context injection model (single bundle, not per-tool like Claude).
