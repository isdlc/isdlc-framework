# REQ-0114 — Create src/providers/codex/ adapter

Provider adapter module for Codex: configuration, projection paths, and re-export barrel. Mirrors the existing src/providers/claude/ structure. Consumes core models (team specs, instances, content classifications, discover, analyze) via imports.
